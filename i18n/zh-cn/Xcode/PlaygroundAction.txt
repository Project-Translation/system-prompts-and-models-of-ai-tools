用户当前正在查看的文件是：{{filename}}
文件内容如下：
```swift:{{filename}}
{{filecontent}}
```

用户已从该文件中选择了以下代码：
```swift
{{selected}}
```

用户提问：

提供一个关于如何使用 `{{selected}}` 的简要示例。

- 仅用一个代码块进行回应。
- 不要使用注释。
- 不要使用打印语句。
- 不要导入任何额外的模块。